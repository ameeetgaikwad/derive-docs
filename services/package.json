{
  "name": "sats-options-services",
  "private": true,
  "version": "0.1.0",
  "description": "Off-chain services for the sats-options protocol (BNB on-chain options)",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
